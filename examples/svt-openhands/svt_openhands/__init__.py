"""SVT OpenHands agent — no framework-specific code needed.

With zil.Session and zil serve, this module is optional.
The agent is fully configured via manifest.yaml + identity/.
Run with: zil serve --project-dir .
"""
